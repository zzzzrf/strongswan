test-runner {                                                                    
	load_modular = yes                                                  
	plugins {  
		random { load = yes }
		qkd {
			# Load the QKD platform plugin (requires --enable-qkd).
			load = yes
			vendor_a {
				mode = 2
				config = custom_config.txt
			}
		}
	}
}